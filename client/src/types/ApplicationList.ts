export interface NFTProcessApplication {
  membershipId: string;
  title: string;
  thumbnailUrl: string;
  seasonStart: string;
  seasonEnd: string;
  status: string;
}
export interface FundingProcessApplication {
  fundingId: string;
  title: string;
  thumbnailUrl: string;
  startDate: string;
  endDate: string;
  status: string;
}
