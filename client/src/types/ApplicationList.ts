export interface NFTProcessApplication {
  membershipId: string;
  title: string;
  imageUrl: string;
  seasonStart: string;
  seasonEnd: string;
  status: string;
}
export interface FundingProcessApplication {
  fundingId: string;
  title: string;
  imageUrl: string;
  startDate: string;
  endDate: string;
  status: string;
}
